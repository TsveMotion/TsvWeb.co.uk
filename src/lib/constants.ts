// Cookie names
export const ADMIN_AUTH_COOKIE = 'admin_auth';
export const SESSION_COOKIE = 'admin_session';

// Session expiration in milliseconds (default: 24 hours)
export const SESSION_EXPIRATION = 24 * 60 * 60 * 1000;
