// Cookie names
export const ADMIN_AUTH_COOKIE = 'admin_auth';
export const SESSION_COOKIE = 'admin_session';

// Session expiration in milliseconds (90 days for better persistence)
export const SESSION_EXPIRATION = 90 * 24 * 60 * 60 * 1000;
